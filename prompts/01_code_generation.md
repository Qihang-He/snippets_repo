# 模板：代码生成（带注释、可运行 Python 脚本）

标题: 代码生成（带注释、可运行 Python 脚本）

目标: ${实现...（例如：计算统计量/绘图）}

输入数据说明: ${示例数据结构或 CSV 列名}

输出要求: ${图像/CSV/摘要}

依赖: ${pandas,numpy,matplotlib}

要求:
1) 返回完整可运行脚本（含 `if __name__ == '__main__'`）。
2) 英文变量名；注释中文。
3) 脚本顶部列出依赖与 pip 安装命令。
4) 提供最小示例数据生成并验证主流程。
